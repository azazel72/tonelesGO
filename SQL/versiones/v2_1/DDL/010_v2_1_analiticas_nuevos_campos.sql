ALTER TABLE `analiticas`
  CHANGE COLUMN `grado_alcoholico` `alcohol` VARCHAR(64) DEFAULT NULL,
  CHANGE COLUMN `acidez_volatil` `av` VARCHAR(64) DEFAULT NULL,
  CHANGE COLUMN `azucar_residual` `azucar` VARCHAR(64) DEFAULT NULL,
  CHANGE COLUMN `temperatura` `ntu` VARCHAR(64) DEFAULT NULL,
  DROP COLUMN IF EXISTS `acidez_total`,
  DROP COLUMN IF EXISTS `so2_libre`,
  DROP COLUMN IF EXISTS `so2_total`,
  ADD COLUMN IF NOT EXISTS `deposito` VARCHAR(255) DEFAULT NULL AFTER `estado`,
  ADD COLUMN IF NOT EXISTS `litros` VARCHAR(64) DEFAULT NULL AFTER `deposito`,
  ADD COLUMN IF NOT EXISTS `ph` VARCHAR(64) DEFAULT NULL AFTER `litros`,
  ADD COLUMN IF NOT EXISTS `numero_botas` VARCHAR(64) DEFAULT NULL AFTER `azucar`,
  ADD COLUMN IF NOT EXISTS `cliente` VARCHAR(255) DEFAULT NULL AFTER `numero_botas`,
  ADD COLUMN IF NOT EXISTS `tipo_bota` VARCHAR(255) DEFAULT NULL AFTER `cliente`,
  ADD COLUMN IF NOT EXISTS `vo_at` VARCHAR(64) DEFAULT NULL AFTER `tipo_bota`,
  ADD COLUMN IF NOT EXISTS `observaciones` VARCHAR(255) DEFAULT NULL AFTER `vo_at`;